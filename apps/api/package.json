{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts"
  },
  "dependencies": {
    "@orpc/server": "^2.0.0-beta.35",
    "@repo/auth-config": "*",
    "@repo/db-config": "*",
    "@repo/orpc-contract-config": "*",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.7",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
